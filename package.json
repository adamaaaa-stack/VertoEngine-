{
  "name": "verto-engine",
  "version": "0.1.0",
  "description": "Web-based game engine with Unreal Blueprint-equivalent node system",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "npm run build && node dist/api.js",
    "serve": "node dist/api.js",
    "test": "echo 'Tests pending'"
  },
  "dependencies": {
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
